# -- FTP credentials (cPanel FTP account) -------------------------------------
# Buat FTP account di cPanel -> FTP Accounts, arahkan ke /home/lakaraid/
FTP_HOST=195.88.211.140
FTP_PORT=21
FTP_USER=ftptrigger@trigger.lakara.id
FTP_PASS=7IY5k7Ov;BrJ8@vH

# FTP_DIR: path di dalam FTP session (relatif terhadap chroot).
# cPanel FTP chroot = /home/lakaraid/  ->  /lakara = /home/lakaraid/lakara di server
FTP_DIR=/lakara

# -- Remote server path -------------------------------------------------------
# Path absolut di server (dipakai untuk perintah remote setelah upload)
REMOTE_DIR=/home/lakaraid/lakara

# -- Webhook trigger (untuk remote command setelah upload) --------------------
# Sudah terkonfirmasi aktif di: https://trigger.lakara.id/index.php
TRIGGER_URL=https://trigger.lakara.id/index.php
SHELL_TOKEN=my-very-secret-token-even-ai-dont-know

# -- cPanel API (tidak dipakai saat ini) -------------------------------------
# CPANEL_USER=
# CPANEL_PASS=